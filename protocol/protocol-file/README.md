# Simple file transfer in local filesystem

Protocol quailifier is `file`.

## Configuration
| Parameter | Explanation |
| -------- | -------- |
| protocol | The protocol, always `file`. |
| directory | The working directory on the share. |

Example:
```
  protocol: file
  directory: /tmp/subfolder
```